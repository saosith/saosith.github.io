---
import { skillGroups } from '../lib/about';

const SKILLS = skillGroups();
---

<section id="skills" class="wrap section rule-top">
  <div class="section-head">
    <span class="idx">07</span>
    <h2>Skills</h2>
    <p class="note">Tools and methods I work with.</p>
  </div>

  <div class="skills">
    {SKILLS.map((g) => (
      <div class="skill-group">
        <h3>{g.name}</h3>
        <ul>
          {g.items.map((i) => <li><span>{i.skill}</span></li>)}
        </ul>
      </div>
    ))}
  </div>
</section>
